# Техническое задания на ветстку страницы о клинике

Необходимо сверстать несколько секций, следующими сообщениями я буду давать задания на верстку той или иной секции. При выполнении заданий учти требования описанные ниже.

## Основные положения

- При верстке используй все современные технологии но только те которые имеют повсеместную поддержку в разных браузерах
- Для стилизации scss
- Стили для каждой секции должны лежать в отдельном файле. Эти файлы должны лежать в директории public\scss\, начинаться с символа '\_' и подключаться в файле public\scss\main.scss
- В начале каждого файла со стилями должно быть подключение файла с переменными, реализованное следующим образом: @use 'vars' as \*;.
- Если в ходе выполнения задания понвдобится вынести часть повторяющихся стилей то сделай отдельный файл и подключи его так чтобы можно было использовать в других фалах стилей.
- При создании стилей используй переменные которые описанны в файле public\scss_vars.scss. Не используй другие имена переменных! Используй только те которые есть в указанном файле.
- Каждая секция должна быть обернута в семантический тег section с осмысленным классом и Id. Внутри каждой секции должен быть контейнер описанный следующим образом <div class="container"> </div>. Всю разметку секции делай внутри данного контейнера.
- Без прямой необходимости или прямых указаний описанных в этом ТЗ не стилизуй секции и вложенный контейнер, стили для них уже прописанны.
- Без указаний в тексте задания не делай эффектов наведения для элементов.
- Если по ходу выполнения работ нужно создать кнопку по умолчанию делай ее при помощи тега a с прописанным классом button. Стили для кнопки уже прописанны.
- Созданные в ходе работы секции помещай в файл resources\views\index.blade.php перед закрывающим тегом @endsection

## Адаптация под устройства (общие требования)

- все секции должны быть адапрированы для мобильных устройств
- при написании стилей адаптации стариайся их минимизировать и не преписывать лишнее
- Мобильное устройство начинается от ширины экрана 520px
- Планшеты начинается от ширины экрана 820px
- Прими во внимание что для слайдеров у которых количество отображаемых элементов на десктоп версии > 1 нужно прописать брекпоинты при инициализации.
- Ниже в задании для каждой секции могут быть описанны требования для ее мобильной адаптации, учитывай эти требования.

Далее буду скидывать отдельные задания на верстку секций.

### Секция - Шапка с логотипом именю

Секция должна иметь верхний сплошной бордер шириной 5px Цветом $main_color. Внутри контейнера секции расположенно 3 элемента:

- Логотип взять из файла - public/img/logo.png Высота логотипа 90px ширина автоматически.
- Меню - для меню сделай контейнер и вставь внутри вызов компонента <x-menues.puncts></x-menues.puncts>
- Контакты- этот блок будет содержать 2 элемента, выровненных по правому краю:
    - Телефон - +7 (4712) 34-30-90 Цвет $main_color шрифт 16px
    - емейл - info@progress-apk.ru Цвет $black_color шрифт 14px

### Секция - Баннер со слайдером

В секции контейнера. Всю секцию занимает слайдер а внутри каждого слайда есть контейнер.

**Параметры для создания слайдера**

- при создании swiper используй подход WebComponent описанный в документации к нему
- инициализировать swipper необходимо с помощью js, написанную инициализацию необходимо поместить в файл resources\js\sliders\main-swiper.js
- в видимой зоне отображаются 1 слайд внутри слайде изображение
- добавь 2 слайда и помести в них изображения public/img/b1_main.webp и public/img/b2_main.webp
- пусть слайдер имеет пагинацию, элементы пагинации размести в центре в низу слайдера и стилизуй с применением цвета $main_color, не активные слайды сделай немного прозрачными
- Внутри каждого слайдера размести контейнер внутри которого будут расположенны следующие элементы:
    - Заголовок h1 с текстом - ЗАО “ПРОГРЕСС” Шрифт: белый жирный 38px
    - Подзаголовок в параграфе с текстом - Точные технологии. Высокие стандарты. Уверенный результат. Шрифт: белый регулар 24px
- Во втором слайде не используй тег h1 просто сделай параграф стилизованный так же как и h1 на первом слайде

### Секция - О компаниях

Внутрь контейнера помести блок который будет разбит на 2 части выстроенных в столбик.

_Верхняя часть разбита на 2 блока:_

- с права фотка со скругленными краями (размер скругления $border_rad_big) возьми возьми из файла - public/img/about_1.webp
- с лева блок с текстовым содержанием:
    - Заголовок h2 с текстом - Профессионализм, проверенный временем Шрифт: жфрный 22px цвет $blue_color
    - hr толщина в 1px цвет $main_color длинна 100px
    - Прпхраф с текстом - Мы строим работу на фундаменте, который не зависит от внешних обстоятельств. Опыт, накопленный за годы практики, позволяет нам сохранять баланс между проверенными решениями и внедрением современных подходов. Результат — стабильность, которую ценят партнеры.

_Нижняя часть разбита на 2 блока:_

- с лева фотка со скругленными краями (размер скругления $border_rad_big) возьми возьми из файла - public/img/about_2.webp

- с лева блок с текстовым содержанием (данный блок имеет заливку цветом $gray_bg_color и скругленные края размер скругления $border_rad_big):
    - Заголовок h2 с текстом - Технологичность Шрифт: жфрный 22px цвет $blue_color
    - hr толщина в 1px цвет $main_color длинна 100px
    - Прпхраф с текстом - Мы строим работу на фундаменте, который не зависит от внешних обстоятельств. Опыт, накопленный за годы практики, позволяет нам сохранять баланс между проверенными решениями и внедрением современных подходов. Результат — стабильность, которую ценят партнеры.

### Секция - Наша компания в цифрах

Данная секция имеет фон - возьми из картинки public/img/numbers.webp

Внутри контейнера секции расположенны следующие элементы:

- заголовок h2 с текстом - Наша компания в цифрах Шрифт: 22px uppercase цвет $blue_color

- блок с карточками 4 в ряд. Карточка имеет скругленные края (размер скругления $border_rad) Залита белым цветом с эффектом матового стекла и небольшой тенбю. Каждая карточка внутри содержит следующие элементы расположенные в столбик:
    - Параграф с цифрой Шрифт: 32px белый жирный
    - hr толщина в 1px цвет $main_color длинна 50px
    - Комментарий Шрифт: 16px белый регулар

_Контент карточек_

| Цифра   | Комментарий                           |
| ------- | ------------------------------------- |
| 20+ лет | Непрерывного развития                 |
| 6 000+  | Посевных площадей                     |
| 70+     | Единиц высокопроизводительной техники |
| 100%    | Обеспеченность энергоресурсами        |

### Подвал

ПОдвал залит белым цветом. Внутри контейнера подвала разположенны 3 блока:

- Блок с логотипом лого возьми из файла public/img/logo.png. Логотип имеет высоту 90px ширина вычисляется автоматически. Он расположен вверхней оевой части блока.
- Блок с меню. В него размести компонент выода меню <x-menues.puncts></x-menues.puncts>. Стилизуй компоненты внутри таким образом чтобы пункты меню распологались в столбик.

- Блок с контактами в котором будут расположенны следующие элементы:
    - заголовок h3 c текстом - Контакты
    - Телефон - +7 (4712) 34-30-90 Цвет $main_color шрифт 16px
    - емейл - info@progress-apk.ru Цвет $black_color шрифт 14px
